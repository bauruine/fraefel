<% content_for :breadcrumb do %>
  <ul class="breadcrumb">
    <li><a href="<%= cargo_lists_path %>">Versand</a> <span class="divider">/</span></li>
    <li><a href="<%= cargo_list_path(@cargo_list) %>"><%= @cargo_list.id %></a> <span class="divider">/</span></li>
    <li class="active">Edit</li>
  </ul>
<% end %>
<div class="page-header">
  <h1>Verdsand</h1>
</div>

<div class="tabbable">
  <ul class="nav nav-tabs">
    <%= content_tag :li, :class => params[:tab].present? ? nil : "active" do %>
      <a data-toggle="tab" href="#tab1">Allgemein</a>
    <% end %>
  </ul>
  <%= render 'edit_form' %>
</div>
