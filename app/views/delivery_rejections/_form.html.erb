<% if @delivery_rejection.errors.present? %>

  <div class="error_messages">
    <h2>Fehlerhafte Eingabe</h2>
    <p>Bitte überprüfen Sie die folgenden Fehler:</p>
    <ul>
      <% @delivery_rejection.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  
  <hr />
<% end %>

<%= simple_form_for(@delivery_rejection, :html => {:class => "form-horizontal"}) do |f| %>
  <div class="tab-content">
    <%= content_tag :div, :id => "tab1", :class => ["tab-pane", params[:tab].present? ? nil : "active"].compact.join(" ") do %>
      <%= render 'delivery_rejections/tabs/new/tab_1', :f => f %>
    <% end %>
    
    <%= content_tag :div, :id => "tab2", :class => ["tab-pane", params[:tab] == "tab2" ? "active" : nil].compact.join(" ") do %>
      <%= render 'delivery_rejections/tabs/new/tab_2', :f => f %>
    <% end %>
    
    <%= content_tag :div, :id => "tab3", :class => ["tab-pane", params[:tab] == "tab3" ? "active" : nil].compact.join(" ") do %>
      <%= render 'delivery_rejections/tabs/new/tab_3', :f => f %>
    <% end %>
    
    <%= content_tag :div, :id => "tab4", :class => ["tab-pane", params[:tab] == "tab4" ? "active" : nil].compact.join(" ") do %>
      <%= render 'delivery_rejections/tabs/new/tab_4', :f => f %>
    <% end %>
  </div>
<% end %>
