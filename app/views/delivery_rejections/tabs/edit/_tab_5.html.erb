<fieldset>
  <legend>Bemerkung</legend>
  <%= f.simple_fields_for :comments do |co_f| %>
    <% unless !co_f.object.new_record? %>
      <%= co_f.input(:content, :label => "Kommentar") %>
    <% end %>
  <% end %>
  <%= render 'delivery_rejections/partials/shared/form_buttons', {:@btn_submit => true, :@btn_next => "#tab6", :@btn_back => "#tab4"} %>
</fieldset>
