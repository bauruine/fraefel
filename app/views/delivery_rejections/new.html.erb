<div class="modal fade" id="search_delivery_rejection_id">
  <div class="modal-header">
    <a class="close" data-dismiss="modal">×</a>
    <h3>Suche Versand</h3>
  </div>
  <%= form_tag(search_delivery_rejections_path, {:method => :post, :remote => true, :class => 'form-horizontal'}) do %>
    <div class="modal-body">
      <div class="control-group">
        <%= label_tag "purchase_order_baan_id", "VK-Auftrag", :class => "control-label" %>
        <div class="controls">
          <%= text_field_tag "purchase_order_baan_id", nil, :class => "text_field" %>
          <p class="help-block"><strong>Info:</strong> Bitte VK-Auftrags Nummer eingeben.</p>
        </div>
      </div>
    </div>
    <div class="modal-footer">
      <%= submit_tag "Suche", :id => "submit", :name => "submit", :class => "btn btn-primary", :disabled => false, :disable_with => "Please wait..." %>
    </div>
  <% end %>
</div>

<ul class="breadcrumb">
  <li>
    <a href="/"><i class="icon-home"></i> Dashboard</a> <span class="divider">/</span>
  </li>
  <li>
    <a href="/delivery_rejections"><i class="icon-folder-open"></i> Konflikte</a> <span class="divider">/</span>
  </li>
  <li class="active">
    <a href="/delivery_rejections/new">Neu</a>
  </li>
</ul>

<%= render 'form' %>